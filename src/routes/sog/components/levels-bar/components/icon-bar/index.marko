$ var cardInfo = input.cardInfo;
$ var imgsrc = input.imgsrc;

style {
    .iconBar {
        background-color: #ffe990;
        text-align: center;
    }
    .sm-icon-bar {
        margin-top: 10px; /*padding for mobile display*/
    }
    @media (max-width: 767px) {
        .md-icon-bar {
            display: none;
        }
    }
    @media (min-width: 768px) {
        .sm-icon-bar {
            display: none;
        }
    } 
}

<table class="md-icon-bar" style="width:100%">
    <th class="iconBar"><img style="max-height:35px" src=imgsrc+"/icons/elements/"+cardInfo.element+".png"></th>
    <th class="iconBar" style="padding:2px"><img style="max-height:55px" src=imgsrc+"/icons/rarity/"+cardInfo.rarity+".png"></th>
    <th class="iconBar" style="padding-top:10px"><img style="max-height:45px;" src=imgsrc+"/icons/health/"+cardInfo.levels[0].health+".png"></th>
    <th class="iconBar" style="padding-top:1px"><img style="max-height:45px" src=imgsrc+"/icons/attack/"+cardInfo.levels[0].attack+".png"></th>
    <th class="iconBar"><img style="max-height:45px" src=imgsrc+"/icons/speed/"+cardInfo.levels[0].speed+".png"></th>
</table>
<table class="sm-icon-bar" style="width:100%">
    <th class="iconBar"><img style="max-height:30px" src=imgsrc+"/icons/elements/"+cardInfo.element+".png"></th>
    <th class="iconBar" style="padding:2px"><img style="max-height:35px" src=imgsrc+"/icons/rarity/"+cardInfo.rarity+".png"></th>
    <th class="iconBar" style="padding-top:10px"><img style="max-height:30px;" src=imgsrc+"/icons/health/"+cardInfo.levels[0].health+".png"></th>
    <th class="iconBar" style="padding-top:1px"><img style="max-height:30px" src=imgsrc+"/icons/attack/"+cardInfo.levels[0].attack+".png"></th>
    <th class="iconBar"><img style="max-height:30px" src=imgsrc+"/icons/speed/"+cardInfo.levels[0].speed+".png"></th>
</table>
